<center>
  <h2><%= @property.title %></h2>

  <%= link_to "Trees", property_trees_path(@property), class: "button small" %>

  <%= link_to "Surveys", property_surveys_path(@property), class: "button small" %>

  <%= link_to "New Survey", new_property_survey_path(@property), class: "button small" %>
  
  <%= link_to "Edit Property", edit_property_path(@property), class: "button small" %>
</center>