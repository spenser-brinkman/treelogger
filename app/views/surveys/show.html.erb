Survey taken on <%= humanize_date(@survey.date) %>
<br>

<%= link_to "Add a new tree to this property's inventory", new_property_tree_path(@survey.property, survey: @survey) %>
<br><br>

<% if @survey.property.trees.any? {|tree| !tree.surveys.include?(@survey)} %>
  These trees have not been logged on this survey yet:<br>
  <% @survey.property.trees.each do |tree| %>
    <% if !tree.surveys.include?(@survey) %>
      <%= link_to tree.name, new_tree_inspection_path(tree, survey: @survey) %> - <%= tree.species.name %><br>
    <% end %>
  <% end %>
<% end %>
<br>
<% if @survey.property.trees.any? {|tree| tree.surveys.include?(@survey)} %>
  Completed inspections:<br>
  <% @survey.inspections.each do |inspection| %>
    <%= link_to inspection.tree.name, tree_path(inspection.tree, survey: @survey) %> - <%= inspection.tree.species.name %><br>
  <% end %>
<% end %>
<br><br>
<%= link_to "Edit this survey", edit_survey_path(@survey) %>